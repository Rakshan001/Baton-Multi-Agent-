# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
*.tsbuildinfo

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env / secrets
.env
.env.*
!.env.example
# web Vite env files hold no secrets (same-origin defaults) — keep them in git
!web/.env.development
!web/.env.production

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Baton runtime state (local handoff history)
.baton/

# Generated knowledge graphs (rebuild with `baton kb init`)
graphify-out/

# Machine-specific MCP config (absolute paths; regenerate with `baton kb init`)
.mcp.json

# Test coverage
coverage/
.refs/

# Local research notes — audits of reference code in .refs/. Not shipped:
# they cite file:line into repos we do not control, so they rot silently and
# would read as project docs to anyone who found them in git.
docs/notes/
